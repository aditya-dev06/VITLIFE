# Workspace Rules

- **Localhost Testing Requirement**: Do not push updates directly to GitHub or deploy to Railway. First allow the user to check and verify the changes on `localhost` (development server). Only commit or push when explicitly instructed by the user.
